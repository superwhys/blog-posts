---
layout:     post
title:      Go 一行代码为何让性能相差 12 倍
subTitle:   从闭包变量逃逸到 CPU 缓存伪共享
date:       2026-07-31
author:     Yong
tags:
    - Go
    - 性能优化
    - 并发
    - 逃逸分析
    - 伪共享
---

## 前言

最近看到一个很有意思的 Go 性能问题：一个使用 Monte Carlo 方法并行估算圆周率的程序，只把 `localCount` 的声明向下移动一行，执行时间就从约 12 秒降到了约 1 秒。

慢版本把计数器声明在 goroutine 外：

```go
for workerID := 0; workerID < numCPU; workerID++ {
    // 每轮循环都会创建一个新的 localCount
    localCount := 0

    wg.Go(func() {
        for i := start; i < end; i++ {
            // 生成随机点……
            if x*x+y*y < 0.25 {
                localCount++
            }
        }
        results <- localCount
    })
}
```

快版本只改了变量的位置：

```go
for workerID := 0; workerID < numCPU; workerID++ {
    wg.Go(func() {
        localCount := 0

        for i := start; i < end; i++ {
            // 生成随机点……
            if x*x+y*y < 0.25 {
                localCount++
            }
        }
        results <- localCount
    })
}
```

两段代码的计算结果和并发安全性没有区别，性能却可能相差一个数量级。原因可以概括为：

> 声明在外部的 `localCount` 被闭包捕获后逃逸到堆，相邻的小对象又可能落在同一个 CPU 缓存行中。多个核心高频写入各自的计数器时，产生了严重的伪共享。

## 第一步：闭包捕获导致变量逃逸

慢版本中的 `localCount` 在 goroutine 外声明，却在闭包中执行 `localCount++`。因为闭包需要修改它，捕获的是变量本身，而不只是一份值拷贝。

goroutine 的结束时间无法在编译期确定，它可能在外层循环甚至 `calc` 函数结束后仍然运行。为了确保 `localCount` 始终有效，编译器不能把它留在外层函数的栈帧中，只能将其移动到堆上。

可以用编译器的逃逸分析验证：

```bash
go build -gcflags='-m=2' main.go
```

慢版本会看到类似输出：

```text
capturing by ref: localCount
localCount escapes to heap
moved to heap: localCount
```

把声明移入 goroutine 后，`localCount` 的生命周期完全包含在该 goroutine 内。它不再被异步闭包从外部捕获，通常可以留在 goroutine 的栈中，热点循环里甚至可能一直保存在寄存器中，直到最后发送结果。

不过，只发生几次堆分配本身不足以解释 12 倍差距。真正昂贵的是逃逸之后的内存布局和数十亿次写操作。

## 第二步：小对象可能被连续分配

在 64 位平台上，`int` 通常只有 8 字节。Go runtime 对不含指针、尺寸小于 16 字节的对象有专门的 tiny allocator，官方源码也明确说明它主要服务于小字符串和独立的逃逸变量。

这些 `localCount` 由启动 worker 的同一个 goroutine 连续创建，因此很可能被紧密地放在同一片堆内存中。示意如下：

```text
低地址                                                       高地址
┌────────┬────────┬────────┬────────┬────────┬────────┬────────┬────────┐
│ count0 │ count1 │ count2 │ count3 │ count4 │ count5 │ count6 │ count7 │
│   8B   │   8B   │   8B   │   8B   │   8B   │   8B   │   8B   │   8B   │
└────────┴────────┴────────┴────────┴────────┴────────┴────────┴────────┘
└────────────────────── 一个 64B 缓存行（示意）──────────────────────┘
```

这不是 Go 语言规范承诺的布局，实际结果会受 Go 版本、分配器状态和硬件影响。但对这种由单个 goroutine 连续产生的微小对象来说，相邻分配是很常见的。

缓存行也不一定都是 64 字节，部分处理器使用更大的缓存行。缓存行越大，同时挤进去的计数器可能越多。

## 第三步：不同变量也会互相拖慢

CPU 缓存并不是以一个 `int` 为单位维护一致性，而是以整个缓存行为单位。

假设 `count0` 和 `count1` 位于同一个缓存行，两个 goroutine 分别运行在不同核心上：

1. 核心 A 修改 `count0`，需要取得该缓存行的写权限；
2. 核心 B 中同一缓存行的副本随之失效；
3. 核心 B 修改 `count1`，又要重新取得缓存行的写权限；
4. 核心 A 中的副本再次失效；
5. 两个核心持续让同一个缓存行来回“乒乓”。

两个 goroutine 明明没有写同一个变量，却因为变量位于同一个缓存行而互相干扰，这就是 **伪共享（false sharing）**。

Monte Carlo 估算中，落入圆内的点约占 `π/4`。也就是说，循环中的大部分迭代都会执行 `localCount++`。当总迭代次数达到 `math.MaxInt32` 时，缓存行所有权可能在核心之间转移数十亿次，性能差距自然会被显著放大。

## 为什么移入 goroutine 就快了

把 `localCount` 声明在 goroutine 内，同时消除了两个问题：

1. **避免逃逸**：计数器可以放在 goroutine 自己的栈中；
2. **避免高频共享写**：编译器还可能把计数器保存在寄存器里，只在结束时写入结果通道。

因此，变化的并不只是源码中的一行位置，而是编译后的存储位置和 CPU 执行方式。

需要注意，12 倍不是一个固定结论。差距会随着 CPU 架构、缓存行大小、核心数量、goroutine 调度、Go 版本和其他堆分配而变化。在某些机器上可能只有数倍，在 goroutine 恰好没有分散到多个核心时也可能不明显。

## 这是不是数据竞争

不是。

外层循环的每次执行都会产生一个独立的 `localCount`，每个计数器只由对应的 goroutine 修改。它们只是物理地址接近，并没有多个 goroutine 同时读写同一个变量。

因此，`go test -race` 通常不会报告问题。Race Detector 检查的是会影响正确性的内存竞争，而伪共享是缓存行级别的性能问题。

## 如何修复

### 方案一：使用 goroutine 的局部变量

这也是本例最简单、最合理的方式：让每个 worker 在内部完成局部聚合，最后只发送一次结果。

```go
wg.Go(func() {
    localCount := 0

    for i := start; i < end; i++ {
        if hit() {
            localCount++
        }
    }

    results <- localCount
})
```

并发程序中，一个很好用的设计原则是：

> 数据尽量由单个 goroutine 独占，在边界处批量合并结果。

### 方案二：只在结束时写共享结果

如果必须使用结果数组，也应先在局部变量中累加，最后只写一次：

```go
counts := make([]int, numCPU)

wg.Go(func() {
    localCount := 0
    for i := start; i < end; i++ {
        if hit() {
            localCount++
        }
    }
    counts[workerID] = localCount
})
```

即使相邻数组元素位于同一个缓存行，一次写入造成的影响也远小于在热点循环中持续写入。

### 方案三：必要时做缓存行填充

如果业务确实要求多个核心持续更新相邻的计数器，可以通过 padding 把它们隔开。例如使用 `golang.org/x/sys/cpu.CacheLinePad`：

```go
import "golang.org/x/sys/cpu"

type paddedCounter struct {
    _     cpu.CacheLinePad
    value int64
    _     cpu.CacheLinePad
}
```

Padding 会增加内存占用，还会引入对具体架构的考虑，应当在基准测试确认存在伪共享后再使用。本例直接把变量移入 goroutine 更清晰，也更高效。

另外，简单地把计数器换成 `atomic.Int64` 并不能解决伪共享。原子操作保证的是读写正确性，不会让相邻变量自动进入不同缓存行，额外的同步语义反而可能让热点更慢。

## 排查类似问题的方法

遇到“代码几乎相同，性能却相差很大”的并发程序，可以按下面的顺序检查：

1. 使用 `go build -gcflags='-m=2'` 检查热点变量是否逃逸；
2. 用 `go test -bench=. -benchmem -count=10` 做多轮基准测试，不要只比较一次运行结果；
3. 检查是否有多个 goroutine 高频修改相邻数组元素、结构体字段或连续分配的小对象；
4. 分别尝试局部聚合、单次写回和 padding，观察性能是否恢复；
5. 在 Linux 上可以进一步使用 `perf c2c` 等工具观察缓存行争用。

## 总结

这次性能差异的完整链路是：

```text
变量声明在 goroutine 外
        ↓
被异步闭包按引用捕获
        ↓
逃逸到堆
        ↓
多个小对象可能被连续分配
        ↓
不同核心高频写入同一缓存行
        ↓
伪共享导致缓存行反复失效
```

所以，堆逃逸只是起点，伪共享才是性能被放大的关键。相比事后添加 padding，更好的思路通常是重新划分数据所有权：让每个 goroutine 使用自己的局部状态，最后再合并结果。

## 参考资料

- [原问题：Go 仅一行代码位置变更，性能差 12 倍，这是为什么？](https://www.zhihu.com/question/1983394937543366516/answer/1984679350000447661)
- [Go GC Guide：Where Go Values Live](https://go.dev/doc/gc-guide#Where_Go_Values_Live)
- [Go runtime 内存分配器源码](https://go.dev/src/runtime/malloc.go)
- [Intel：False Sharing](https://www.intel.com/content/www/us/en/docs/vtune-profiler/cookbook/2024-2/false-sharing.html)
